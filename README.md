# Property-Price-Predictor
This project implements a machine learning pipeline to predict property prices based on features like location, city, property type, number of bedrooms, bathrooms, and area. It uses an XGBoost regressor with preprocessing handled via a Scikit-learn pipeline. The model predicts log-transformed prices and returns a realistic price range.
